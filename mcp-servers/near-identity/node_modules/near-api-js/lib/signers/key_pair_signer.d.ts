import { KeyPair, type KeyPairString, type PublicKey } from '../crypto/index.js';
import { Signer } from './signer.js';
/**
 * Signs using in KeyPair.
 */
export declare class KeyPairSigner extends Signer {
    private readonly key;
    constructor(key: KeyPair);
    static fromSecretKey(encodedKey: KeyPairString): KeyPairSigner;
    getPublicKey(): Promise<PublicKey>;
    protected signBytes(bytes: Uint8Array): Promise<Uint8Array>;
}
