import { PublicKey } from '../crypto/public_key.js';
import type { Provider } from '../providers/provider.js';
import { type MessagePayload } from './schema.js';
export interface VerifyMessageArgs {
    signerAccountId: string;
    signerPublicKey: string | PublicKey;
    payload: MessagePayload;
    signature: Uint8Array;
    provider: Provider;
}
export declare function verifyMessage({ signerAccountId, signerPublicKey, payload, signature, provider, }: VerifyMessageArgs): Promise<void>;
