export function signMessage({ signerAccount, payload }) {
    const signer = signerAccount.getSigner();
    if (!signer)
        throw new Error('Please set a signer');
    return signer.signNep413Message(signerAccount.accountId, payload);
}
