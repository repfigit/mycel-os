import { type Schema } from 'borsh';
export interface MessagePayload {
    message: string;
    recipient: string;
    nonce: Uint8Array;
    callbackUrl?: string;
}
export declare const Nep413MessageSchema: Schema;
export declare function serializeMessage(payload: MessagePayload): Uint8Array;
