import type { JsonRpcRequest, JsonRpcResponse } from './methods.js';
interface BackOffOptions<E> {
    numOfAttempts: number;
    timeMultiple: number;
    startingDelay: number;
    retry: (error: E) => boolean;
}
export declare function retryConfig(numOfAttempts?: number, timeMultiple?: number, startingDelay?: number): BackOffOptions<ProviderError>;
export interface ConnectionInfo {
    url: string;
    headers?: {
        [key: string]: string | number;
    };
}
export declare class ProviderError extends Error {
    cause: number;
    constructor(message: string, options: any);
}
/**
 * Performs an HTTP request to an RPC endpoint
 * @param url URL for the HTTP request
 * @param json Request body
 * @param headers HTTP headers to include with the request
 * @returns Promise<any> }arsed JSON response from the HTTP request.
 */
export declare function fetchJsonRpc<Req extends JsonRpcRequest>(url: string, json: Req, headers: object, retryConfig: BackOffOptions<ProviderError>): Promise<JsonRpcResponse<Req['method']>>;
export {};
